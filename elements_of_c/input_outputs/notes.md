# inputs and outputs
## conversion specification for printf
* %c = print a single character
* %d, %i = print a decimal integer
* %u = print an unsigned decimal integer
* %o = print an unsigned octal integer
* %x = print an unsigned hexadecimal integer using a, b, c, d, e, f
* %X = print an unsigned hexadecimal integer using A, B, C, D, E, F
* %f = print a floating point number(by default 6 digits printed after the decimal point)
* %e = print a floating point number in exponential format
* %E = print a floating point number in exponential format in capital E
* %G = print a floating point number in %f or %e format whichever is shorter
etc., like %s, %g, %%, %p

## Conversion specification for scanf()
refer the  CinDepth book

## Reading Input Data
* scanf("control string", address1, address2);
